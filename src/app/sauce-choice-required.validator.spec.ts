import { sauceChoiceRequiredValidator } from './sauce-choice-required.validator';

describe('SauceChoiceRequiredValidator', () => {
  it('should create an instance', () => {
    expect(sauceChoiceRequiredValidator).toBeTruthy();
  });
});
